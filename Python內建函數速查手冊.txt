# 檔名：Python內建函數速查手冊

**一言以蔽之**
這是一份Python 3.11.14版本中，所有內建函數的詳盡參考指南，涵蓋其功能、參數、用法及範例。

**關鍵重點**
*   **全方位列表**：收錄了Python 3.11.14版本中所有內建函數，並按字母順序列出。
*   **詳細說明**：每個函數都附有清晰的功能解釋、參數說明、返回值以及常見使用範例。
*   **易於查找**：內容結構化，方便使用者快速定位所需函數。
*   **版本資訊**：標註了部分函數新增或變更的版本（如新版、已變更）。
*   **實用連結**：提供相關主題的連結，便於深入學習。

**詳細摘要**
本文件為Python 3.11.14版本的官方文件節選，專門整理了Python直譯器內建的所有函數。這些函數是Python語言的核心組成部分，無需額外匯入即可直接使用。

**核心內容結構：**

1.  **函數列表**：文件開頭提供了一個按字母順序排列的完整內建函數列表，方便使用者快速瀏覽和查找。例如，`A`開頭的函數有`abs()`, `aiter()`, `all()`, `anext()`, `any()`, `ascii()`。
2.  **函數詳解**：列表後的內容則針對每一個函數進行詳細介紹。對於每個函數，都包含了：
    *   **函數簽名**：展示函數的名稱及參數（例如 `abs(x)`）。
    *   **功能描述**：清楚說明該函數的作用。例如，`abs(x)` 用於回傳數字的絕對值。
    *   **參數說明**：解釋每個參數的意義、類型和預設值（若有）。
    *   **返回值**：說明函數執行後會回傳什麼。
    *   **範例**：提供實際的程式碼範例，展示函數的用法和預期輸出，讓學習者更容易理解。
    *   **版本變更**：部分函數會標註其新增或變更的Python版本，例如 `aiter()` 和 `anext()` 是在3.10版本中新增的。
    *   **注意事項/警告**：針對特定函數可能存在的陷阱或限制進行提醒，例如 `compile()` 的警告，以及 `round()` 的浮點數行為。
    *   **相關函數/概念**：連結到其他相關的內建函數、模組或概念，例如 `bin()` 提及 `format()`，`hex()` 提及 `format()` 和 `float.hex()`。

**重點函數類別範例：**

*   **數值處理**：`abs()`, `bin()`, `complex()`, `float()`, `hex()`, `int()`, `max()`, `min()`, `oct()`, `pow()`, `round()`, `sum()`。
*   **資料型態轉換與創建**：`bool()`, `bytearray()`, `bytes()`, `dict()`, `frozenset()`, `list()`, `set()`, `str()`, `tuple()`。
*   **迭代與序列操作**：`enumerate()`, `filter()`, `iter()`, `len()`, `map()`, `next()`, `range()`, `reversed()`, `slice()`, `sorted()`, `zip()`。
*   **物件與屬性操作**：`callable()`, `delattr()`, `dir()`, `getattr()`, `hasattr()`, `hash()`, `id()`, `isinstance()`, `issubclass()`, `object()`, `setattr()`, `type()`, `vars()`。
*   **輸入/輸出與執行**：`eval()`, `exec()`, `open()`, `print()`。
*   **類別與裝飾器**：`classmethod()`, `property()`, `staticmethod()`, `super()`。
*   **輔助與系統**：`breakpoint()`, `compile()`, `help()`, `input()`, `__import__()`。

這份文件是Python開發者必備的參考工具，能幫助快速掌握和運用Python強大的內建功能。